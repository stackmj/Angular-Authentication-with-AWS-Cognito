import { IsAuthorizeDirective } from './is-authorize.directive';

describe('IsAuthorizeDirective', () => {
  it('should create an instance', () => {
    const directive = new IsAuthorizeDirective();
    expect(directive).toBeTruthy();
  });
});
